import ReactDOM from 'react-dom';
import App from "./App"
import Estilo from './../src/globalstyle'
const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
   <> 
      <App/>
      <Estilo/>
   </>
);